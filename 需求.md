# 5gipc-rc 需求说明（方案 2 定稿，2026-09-11）

> 参考项目: `已完成项目/RCfpv5.01公网`（功能基线，已实机验证）
> 架构: **edge(WebRTC) 全量跑在 OpenIPC 主摄上，视频/对讲/数控 WebRTC P2P 直达地面端**
> 前期方案 B（VPS 跑 edge）已跑通，但多一跳公网延迟偏高；方案 2 目标 = 真 P2P + 省 VPS 带宽。

---

## 1. 项目概述

把参考项目中的 Radxa 车端主机去掉，由 **OpenIPC 主摄(ssc338q+IMX335) 直接承担 edge 全部功能**（WebRTC P2P 推流 + 串口数传 + 本地声卡对讲），地面端复用参考项目 PC 桌面站（零改动）。

**当前实际设备（用户确认）**：主摄 1 台 + 飞控 + 双 USB 声卡（card0 麦克风 / card1 扬声器）+ PC 地面端 + VPS(信令/TURN)；**副摄像头暂不接（二期）**。

---

## 2. 系统功能基线（参考项目已验证，全部保留）

### 2.1 组成

| 端 | 参考项目实现 | 方案 2 承担者 |
|---|---|---|
| edge | Radxa Zero3W（C++/libdatachannel） | **OpenIPC 主摄（ARMv7 交叉编译）** |
| 摄像头 | IPC ×2 | OpenIPC ×2（副摄二期） |
| 飞控 | MAVLink2（UDP/串口） | 串口 ttyS2@115200 |
| USB 声卡 | plughw:1,0 全双工 | card0 mic mono / card1 speaker stereo 48k |
| 云端 | signaling + coturn | VPS（仅信令/TURN，**不中转媒体**） |
| PC 地面站 | Electron WebRTC | 零改动复用 |

### 2.2 功能基线（全部保留）

1. **双摄视频**：主/副 RTSP H.265（副摄二期）
2. **WebRTC 推流**：主视频轨 H265 + 副视频轨 PIP + Opus 音频轨 + JSON 数据通道
3. **MAVLink2 桥**：串口/ UDP；遥测下行 JSON；命令上行；FWD_TARGETS 转发；GCS 直连 14550
4. **双向对讲**：本地声卡采集(48k/mono) → Opus 上行；下行 Opus → 声卡播放（card1 stereo）；MIC 增益/采集音量/扬声器音量远程调节
5. **看门狗**：数据通道断链 >5s → 中立 RC_OVERRIDE + 停心跳 → 飞控 failsafe 停车
6. **命令集**：SET_MODE/ARM/DISARM/SET_SPEED/SET_ALT/SET_RADIUS/DO_CHANGE_SPEED/RC_OVERRIDE/FC_CONNECTION/FWD_TARGETS/SET_MAIN_URL/SET_AUX_URL/SET_MIC_GAIN/SET_MIC_HW_VOLUME/SET_VOLUME 等

### 2.3 地面站功能清单（原样复用）

WebRTC 收流/H.265 硬解/主副 PIP/OSD/手柄键盘 20Hz/地图/i18n/持久化。**唯一改动**：信令地址指向本 VPS + room_id 独立（如 `rcfpv5-rc`）。

### 2.4 信令与云端

signaling(WSS 8443) + coturn TURN(3478, 49152-65535)——**只做信令和打洞兜底，媒体默认 P2P**。

### 2.5 数据通道协议（复用，不改变）

- 下行：`t:"tel"`/`param`/`fc_connection`/`fwd_targets`/`volume`/`aux_video`/`main_video`/`mic_gain`/`mic_hw_volume`
- 遥测字段：姿态/GPS/电压电流/高度/速度/航向/油门/RSSI/卫星数/定位/原点
- 上行：见 2.2 命令集

---

## 3. 目标系统（5gipc-rc）

### 3.1 与参考项目的差异

| 项 | 参考项目 | 5gipc-rc |
|---|---|---|
| edge 位置 | Radxa（arm64, 2GB+） | **OpenIPC 主摄（ARMv7, 91MB）** |
| 摄像头 | IPC ×2 | OpenIPC ×2（副摄二期） |
| 视频链路 | 局域网 RTSP → edge → WebRTC | **主摄本地取流 → WebRTC P2P → 地面端**（打洞失败走 TURN） |
| 对讲 | ALSA 全双工 | 双卡（card0 mic / card1 speaker） |
| 地面端 | PC + 手机 | 仅 PC，零改动 |
| 云端 | 信令 + TURN | 同（不中转媒体） |

### 3.2 硬件组成

```
[飞控 UART]──ttyS2──┐
[USB mic card0]─────┤ 主摄 OpenIPC(ssc338q+IMX335)
[USB spk card1]─────┤   运行: majestic(编码) + edge(WebRTC) + mavfwd + S60sound
                     │    网线──[5G CPE]──蜂窝──[VPS 信令/TURN]
[副摄 OpenIPC]──────┘（二期）
                           └──WebRTC P2P──[PC 地面端]
```

### 3.3 编码基线

| 路 | 参数 |
|---|---|
| 主摄 | H.265 1080p30 CBR 2048kbps（低延迟模式；内存紧张时可 720p） |
| 副摄（二期） | H.265 720p30 CBR 1024kbps |
| 对讲 | Opus 48k/单声道/20ms(960)；播放端转 stereo 48k |

---

## 4. 功能需求（按功能域，标注实现方）

> 实现方：📷 主摄(edge/majestic) ｜ 💻 地面站（复用）

### 4.1 视频

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| V1 | 主摄画面 | 1080p30 H.265 majestic 编码 → 本地 edge 取流(RTSP/RTP) → WebRTC P2P → 地面站硬解 | 📷+💻 |
| V2 | 副摄画面（二期） | 720p30，PIP 小窗 | 📷+💻 |
| V3 | 源切换 | SET_MAIN_URL/SET_AUX_URL 运行时切换+持久化 | 💻→📷 |
| V4 | 断线自愈 | RTSP/信令/ICE 断线自动重连 | 📷 |
| V5 | 延迟 | 目标 ≤250ms（P2P 无中继，5G 蜂窝物理下限 ~150ms） | 全链路 |

### 4.2 数传与控制（MAVLink2）

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| M1 | 数传链路 | 飞控 ↔ ttyS2@115200 ↔ MavlinkBridge(串口) ↔ 数据通道 ↔ 地面站 | 📷+💻 |
| M2 | 遥测下行 | 姿态/GPS/电压/高度/速度/航向/油门/RSSI/卫星数 → OSD | 📷+💻 |
| M3 | 控制上行 | 模式/ARM/DISARM/速度/高度/半径/RC_OVERRIDE(20Hz) | 💻→📷 |
| M5 | 遥测转发 | FWD_TARGETS 转发（QGC 等） | 📷 |
| M6 | 心跳 | 1Hz GCS 心跳 | 📷 |

### 4.3 双向对讲（本地声卡，PTT）

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| A1 | 上行 | card0 mic 48k/mono → Opus → WebRTC 音频轨 → 地面站 | 📷+💻 |
| A2 | 下行 | 地面站 mic → WebRTC → edge → Opus 解码 → 转 stereo → card1 播放 | 💻+📷 |
| A3 | PTT | 按住说话 | 💻 |
| A4-A6 | 调节 | MIC 增益 0-200% / 采集音量 0-100%(关AGC) / 扬声器音量 0-100%，即时生效+持久化 | 💻→📷 |

### 4.4 安全看门狗

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| W1 | 停车保护 | 数据通道断链 >5s → 中立 RC_OVERRIDE + 停心跳 → failsafe 停车 | 📷 |

### 4.5 地面端（复用，零改动）

| # | 功能 | 说明 |
|---|---|---|
| G1 | 全功能复用 | 2.3 清单全量 |
| G2 | 唯一改动 | 信令地址 + room_id 独立 |

### 4.6 部署与运维

| # | 项 | 说明 |
|---|---|---|
| D1 | 主摄固件 | 自编固件：USB Audio 驱动内置 + **edge/mavfwd/监护开机自启** + 砍包瘦身（去 zerotier/webui/wifi），刷机即用 |
| D2 | VPS | 仅 signaling + coturn（不跑 edge，不中转媒体） |
| D3 | 进程监护 | 5gipc-monitor 监护 mavfwd/edge（等 WG 就绪后拉起） |

---

## 5. 非功能需求

| 项 | 要求 |
|---|---|
| 延迟 | ≤250ms（P2P），5G 蜂窝物理下限 ~150ms |
| 断链恢复 | RTSP 重连 / 信令重连 / ICE 重协商 |
| 安全 | 媒体 SRTP/DTLS；命令走数据通道；公开端口仅信令/TURN |
| 内存 | 91MB 硬约束；实测基线 **MemAvailable=60MB**，edge 需 ≤35MB |
| 部署 | 固件刷机即用，不依赖 SD 卡 |

---

## 6. 关键约束（OpenIPC 主摄）

| 约束 | 说明 |
|---|---|
| RAM 91MB | 实测 majestic 仅 8.4MB、可用 60MB；edge(libdatachannel) 需实测 ≤35MB |
| NOR 16MB | kernel 2MB / rootfs ≤8MB；砍包瘦身腾空间 |
| 无 opkg | 依赖编进固件或静态分发 |
| 无现成 ALSA | 内核 USB Audio 驱动内置（.ko 已验证） |
| 无 libdatachannel | ARMv7 交叉编译（mbedtls+usrsctp+srtp 静态） |
| busybox | 守护脚本 ash 兼容 |

---

## 7. 方案定稿：edge 在摄像头，WebRTC P2P（方案 2）

### 7.1 决策结论（2026-09-11）

前期方案 B（VPS 跑 edge）**已跑通**（视频/数传/对讲全通），但视频经 VPS 中转延迟 0.2-0.5s。方案 2 将 edge 移回摄像头：
- ✅ **真 P2P**：媒体不经 VPS，延迟目标 ≤250ms
- ✅ **省 VPS 带宽**：不再双份转发
- ✅ **内存关已过**：实测 MemAvailable=60MB，majestic 仅 8.4MB
- ❌ 需交叉编译 libdatachannel + 全量移植 edge（工作量集中在摄像头侧）

### 7.2 拓扑与端口

```
主摄(ssc338q): majestic(554) + edge(libdatachannel, 信令 WSS→VPS)
   ├ 视频: 本地取流 → WebRTC P2P（打洞失败 → VPS TURN:3478）
   ├ 对讲: 本地 ALSA(card0/card1) + Opus → WebRTC 音频轨
   └ 数控: ttyS2 → MavlinkBridge(串口) → 数据通道
VPS: signaling(WSS 8443) + coturn TURN(3478) —— 只信令/兜底，不中转媒体
```

| 端口 | 用途 | 绑定 |
|---|---|---|
| 8443/tcp | WSS 信令 | VPS |
| 3478/udp+tcp, 49152-65535/udp | TURN 兜底 | VPS |
| 554/tcp | majestic RTSP（本地取流） | 主摄 |

### 7.3 各端改造点

**主摄（核心开发）**：
- **edge 移植**：参考项目 edge/src 原版（AudioCapture/AudioPlayer 本地 ALSA、MavlinkBridge 串口、RtspCapture 本地拉流）+ 内存裁剪（FFmpeg 换轻量取流可选）
- 交叉编译 libdatachannel(ARMv7, mbedtls) + edge 静态二进制 → 内置固件 overlay
- 固件：USB Audio 驱动 + 砍包瘦身 + S60sound/S99_5gipc 自启

**VPS**：signaling + coturn（复用参考项目 cloud_setup.sh），不跑 edge

**地面端**：零改动，仅信令/room_id

**副摄（二期）**：独立 WG 或同网段，edge 拉第二路 RTSP

### 7.4 风险与对策

| 风险 | 对策 |
|---|---|
| 91MB 内存不足 | 实测可用 60MB；edge 裁剪（静态 mbedtls、轻量取流）；不行降 720p |
| 5G CPE NAT 打洞失败 | WebRTC 自动降级 TURN 中继（仍可用，延迟略增） |
| 交叉编译工作量大 | 分步：edge_skel 验证 → Audio/Video 单轨 → 全量 |
| 蜂窝丢包 | WebRTC NACK/PLI；后续可评估 SRT 混合（二期） |

---

## 8. 范围外（本期不做）

- 手机 Flutter 地面端
- 副摄（二期）
- RTSP 输出 :8554 / GCS 视频转发 :5600（用户取消）
- SRT 传输升级（待交叉编译 armv7 ffmpeg+libsrt，另行评估）
- 方案 B（VPS edge）代码已清理，README 保留历史记录

---

## 9. 实施进度（2026-09-11）

### 9.1 已完成

| 项 | 状态 | 说明 |
|---|---|---|
| 声卡可行性 | ✅ 实机 | card0 mic / card1 speaker 全通 |
| USB 音频驱动 | ✅ 编译验证 | GH Actions `snd-modules` |
| 内存基线 | ✅ 实测 | **MemAvailable=60MB，majestic RSS 8.4MB** → 方案 2 内存关通过 |
| 固件定制 | ✅ 代码就绪 | `firmware/`：瘦身 defconfig + 内核声卡 + overlay 内置服务 |
| edge 骨架 | 🔄 编译中 | `edge-skel/`：ARMv7 libdatachannel + edge_skel 内存实测 |
| 方案 B 代码 | ✅ 已清理 | vps-edge/、audio_bridge 等已删除 |

### 9.2 待办（按序）

1. build-edge-skel 编译通过 → 设备实测 VmPeak（坐实 ≤35MB）
2. **edge 全量移植**（参考项目 src 交叉编译 + 内存裁剪）
3. VPS 部署 signaling + coturn
4. 固件构建（`make BOARD=ssc338q_rc`）+ 刷机
5. 端到端 P2P 联调（视频/数传/对讲/看门狗）+ 延迟实测 ≤250ms

### 9.3 关键实测结论（固化）

- 内存：**91MB 总 / 60MB 可用 / majestic 8.4MB / audio_bridge 1.8MB**（方案 B 遗留进程已删除）
- 声卡：card0 mic mono 48k / card1 speaker stereo 仅 48k / plughw 不可用
- 代理：git push 需 `http.sslBackend openssl` + 系统代理 127.0.0.1:7897

# 5gipc-rc 需求说明（整理稿）

> 版本: v0.1（需求整理，待评审）
> 参考项目: `已完成项目/RCfpv5.01公网`（功能基线，已实机验证）
> 本文件只描述"要什么"，不包含实现方案定稿（关键决策见第 7 章）

---

## 1. 项目概述

把已完成的 **RCFpv5.01公网**（5G 无人车 FPV 系统）中的 **Radxa Zero3W 车端主机去掉**，
由 **OpenIPC 摄像头（ssc338q+IMX335）直接承担车端（edge）全部功能**。

- 功能要求与参考项目 **完全一致**
- 地面端 **复用参考项目 PC 桌面地面站**（APFPV 地面站，Electron），仅信令地址/房间号不同
- 摄像头 **两个都是 OpenIPC**（主摄承载飞控/声卡/edge 功能，副摄提供第二路视频）
- 机位其余设备与参考项目相同：飞控（MAVLink2）、USB 声卡、5G CPE

---

## 2. 参考系统功能基线（RCFpv5.01公网）

### 2.1 系统组成

| 端 | 硬件 | 软件 |
|---|---|---|
| 车端 edge | Radxa Zero3W（RK3566, arm64） | C++：RTSP→WebRTC 推流、MAVLink2 桥、ALSA 对讲、RTSP 输出 :8554、GCS 视频转发 |
| 摄像头 ×2 | IPC 摄像头（RTSP H.265，主 192.168.1.119） | — |
| 飞控 | MAVLink2（UDP 推流到板 :5760 或串口 115200） | — |
| USB 声卡 | Generalplus（48k/mono/960，全双工） | /etc/asound.conf |
| 云端 | 公网 VPS | signaling(Node.js WSS 8080/8443) + coturn TURN(3478, 49152-65535) |
| PC 地面站 | Windows 10+ | Electron + React：WebRTC 收流、H.265 硬解、OSD、控制、地图 |

### 2.2 系统功能基线（参考项目已验证，全部保留）

1. **双摄视频采集**：主/副两路 RTSP H.265 拉流（TCP 传输）；`SET_MAIN_URL`/`SET_AUX_URL` 运行时切换并持久化；副路可停用
2. **WebRTC 推流**（P2P，信令服务器房间制）：
   - 主视频轨（H.265）
   - 副视频轨（mid=`video2`，双摄 PIP）
   - 音频轨（Opus 编码）
   - 数据通道（JSON 文本协议）
3. **MAVLink2 桥**：UDP :5760 或串口(115200)；遥测→数据通道 JSON 下行；数据通道命令→MAVLink 上行；`FWD_TARGETS` 遥测转发（QGC）；GCS 直连 :14550
4. **双向对讲**：USB 声卡采集(48k/mono/960)→Opus→上行；下行 Opus→声卡播放；`SET_MIC_GAIN`(0~200%)/`SET_MIC_HW_VOLUME`(0~100%)/`SET_VOLUME` 远程调节并持久化
5. **链路看门狗**：地面站数据通道断链 >5s → 发一次中立 RC_OVERRIDE + 停止心跳（触发飞控 failsafe 停车）
6. **命令集**：SET_MODE / ARM / DISARM / SET_SPEED / SET_ALT / SET_RADIUS / DO_CHANGE_SPEED / RC_OVERRIDE(16通道) / FC_CONNECTION / GET_FC_CONNECTION / FWD_TARGETS / GET_FWD_TARGETS / SET_AUX_URL / GET_AUX_URL / SET_MAIN_URL / GET_MAIN_URL / SET_MIC_GAIN / GET_MIC_GAIN / SET_MIC_HW_VOLUME / GET_MIC_HW_VOLUME / SET_VOLUME / GET_VOLUME

### 2.3 PC 地面站功能清单（原样复用）

- WebRTC P2P 收流（viewer 应答 edge 的 offer），H.265 硬解（HEVC 视频扩展），主/副双摄 PIP
- OSD：状态/姿态球/网络信号等，随视频区宽度动态缩放
- 控制：手柄/键盘 MANUAL_CONTROL(20Hz)、RC 通道条、模式切换、ARM/DISARM、速度/高度/半径设置
- 地图：可拖拽、WGS84→GCJ-02 纠偏
- 配置面板：连接设置（信令地址/房间）、摄像头配置（主/副 URL）、音频配置（麦克风增益/硬件音量/扬声器音量）、RC 配置
- 中英双语（i18n）、本地设置持久化
- **唯一改动**：信令地址与 room_id 指向 5gipc-rc 的部署（room_id 用独立值，避免与参考项目视频串台）

### 2.4 信令与云端

- signaling：Node.js + ws，HTTP 8080 / HTTPS 8443（自签证书 `disableTlsVerify`）
- coturn TURN：3478/udp+tcp + 49152-65535/udp 媒体转发（公网 NAT 穿透）
- 两种模式：局域网直连 `ws://<IP>:8080` / 公网 `wss://<云IP>:8443`

### 2.5 数据通道协议（复用，不改变）

- 下行：`t:"tel"` 遥测 / `t:"param"` 参数 / `t:"fc_connection"` / `t:"fwd_targets"` / `t:"volume"` / `t:"aux_video"` / `t:"main_video"` / `t:"mic_gain"` / `t:"mic_hw_volume"` / `t:"video_targets"`
- 遥测字段：custom_mode / base_mode / 电压电流电量 / GPS(1e7) / 高度 / 空速地速 / 航向 / 爬升 / 油门 / 姿态(rad) / RSSI / 卫星数 / 定位类型 / 原点经纬高
- 上行：见 2.2 第 6 条命令集

---

## 3. 目标系统（5gipc-rc）

### 3.1 与参考项目的差异

| 项 | 参考项目 | 5gipc-rc | 影响 |
|---|---|---|---|
| 车端主机 | Radxa Zero3W（arm64，2GB+ RAM） | **取消** | **edge 全部功能由 VPS 承担（方案 B）**；主摄只做采集/数传/音频 |
| 摄像头 | 普通 IPC ×2 | **OpenIPC 摄像头 ×2** | 主摄承载飞控(ttyS2)+USB声卡+WG 隧道；副摄纯视频源 |
| 地面端 | PC + 手机 | **仅 PC 桌面端** | 复用，不改功能 |
| 房间号 | rcfpv5-default | 独立值（如 `rcfpv5-rc`） | 防串台 |
| 信令/云端 | 自有 VPS + coturn | 复用 182.92.77.180（新增 signaling/edge） | 部署层面 |

### 3.2 硬件组成（机位）

```
[飞控 UART]──ttyS2──┐
[USB 声卡×2]──USB host─┤ 主摄 OpenIPC(ssc338q+IMX335) ──网线──[5G CPE]──蜂窝──[VPS]
[副摄 OpenIPC]──┴───────────────同网段/独立WG────────┘      │ edge + signaling + TURN
                                                           └──公网──[PC 桌面地面站]
```

- 主摄 OpenIPC：majestic 编码 + mavfwd(ttyS2) + audio_bridge(USB 声卡) + WG 隧道；**不跑 WebRTC edge**
- 副摄 OpenIPC：第二路视频源（majestic RTSP），独立 WG 隧道
- VPS 经 WG 直接拉两路 RTSP（`10.55.0.2` / `10.55.0.3`）
- 主摄内核需内置 USB Audio 驱动（自编固件，.ko 已验证）

### 3.3 视频/编码基线（沿用工程约定）

| 路 | 参数 |
|---|---|
| 主摄 | H.265 1080p30 CBR 2048kbps，低延迟模式 |
| 副摄 | H.265 720p30 CBR 1024kbps（OpenIPC 侧可再调） |
| 对讲 | Opus，48k/单声道/20ms(960)（与地面端协议一致） |

---

## 4. 功能需求（按功能域，标注实现方）

> 实现方：🖥 VPS edge ｜ 📷 主摄 OpenIPC ｜ 📷2 副摄 ｜ 💻 地面站（复用）

### 4.1 视频（双摄）

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| V1 | 主摄画面 | 1080p30 H.265，majestic 硬件编码 → RTSP → VPS 拉流 → WebRTC → 地面站硬解全屏显示 | 📷+🖥+💻 |
| V2 | 副摄画面 | 720p30 H.265，同链路，地面站 PIP 小窗叠加显示 | 📷2+🖥+💻 |
| V3 | 视频源切换 | 地面站可改主/副视频源地址（SET_MAIN_URL/SET_AUX_URL），立即生效并持久化；副路可停用（清空地址） | 💻→🖥 |
| V4 | 断线自愈 | RTSP 断线自动重连（摄像头重启/网络抖动自动恢复）；WebRTC 掉线自动重连 | 🖥 |
| V5 | 延迟 | 端到端百毫秒级（5G 公网放宽到秒级可接受，实测为准） | 全链路 |

### 4.2 数传与控制（MAVLink2）

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| M1 | 数传链路 | 飞控 ↔ ttyS2@115200 ↔ mavfwd → WG → VPS MavlinkBridge(UDP 7212) ↔ 数据通道 ↔ 地面站 | 📷+🖥+💻 |
| M2 | 遥测下行 | 姿态/GPS/电压电流/高度/速度/航向/油门/RSSI/卫星数等 → 地面站 OSD 实时显示 | 🖥+💻 |
| M3 | 控制上行 | 模式切换、ARM/DISARM、速度/高度/半径、DO_CHANGE_SPEED、RC_OVERRIDE(手柄/键盘 20Hz) | 💻→🖥 |
| M4 | 飞控连接管理 | FC_CONNECTION 切换 UDP/串口，查询连接状态（回显 t:"fc_connection"） | 💻→🖥 |
| M5 | 遥测转发 | FWD_TARGETS 设置转发目标（如 QGC），双向透传 | 🖥 |
| M6 | 心跳 | edge 以 1Hz 发 GCS 心跳维持飞控上行 | 🖥 |

### 4.3 双向对讲（USB 声卡，PTT）

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| A1 | 麦克风上行 | card0(mic) 采集 48k/mono → Opus 编码 → UDP → VPS → WebRTC 音频轨 → 地面站播放 | 📷+🖥+💻 |
| A2 | 扬声器下行 | 地面站 mic → WebRTC → VPS → UDP → audio_bridge → Opus 解码 → **转 stereo** → card1 播放 | 💻+🖥+📷 |
| A3 | 对讲控制 | PTT 按住说话（地面站控制说话方向） | 💻 |
| A4 | 麦克风增益 | SET_MIC_GAIN 0~200%（软件增益），立即生效+持久化，回显 | 💻→📷 |
| A5 | 采集音量 | SET_MIC_HW_VOLUME 0~100%（card0 mixer，关 AGC），立即生效+持久化 | 💻→📷 |
| A6 | 扬声器音量 | SET_VOLUME 0~100%（软件音量），立即生效 | 💻→📷 |

### 4.4 链路安全看门狗

| # | 功能 | 具体行为 | 实现方 |
|---|---|---|---|
| W1 | 停车保护 | 数据通道断链 >5s → 发一次中立 RC_OVERRIDE(16ch 全 1500) + 停止心跳 → 飞控 failsafe 停车 | 🖥 |

### 4.5 地面端（复用，零改动）

| # | 功能 | 具体行为 |
|---|---|---|
| G1 | 全功能复用 | 2.3 清单全量（WebRTC 收流/H.265 硬解/OSD/控制/地图/i18n/持久化） |
| G2 | 唯一改动 | 信令地址 `wss://182.92.77.180:8443/signaling` + room_id 独立（如 `rcfpv5-rc`） |

### 4.6 部署与运维

| # | 项 | 说明 |
|---|---|---|
| D1 | 主摄固件 | 自编固件：USB Audio 驱动内置 + audio_bridge/mavfwd/5gipc 服务开机自启，刷机即用，不依赖 SD 卡 |
| D2 | 副摄固件 | 纯视频源 + WG，无飞控/声卡/5gipc 服务 |
| D3 | VPS 服务 | signaling + coturn + edge 全 systemd 自启，端口 8443/3478/7212/7213 |
| D4 | 进程监护 | 主摄 5gipc-monitor 监护 audio_bridge/mavfwd（等 WG 就绪后拉起） |

---

## 5. 非功能需求

| 项 | 要求 |
|---|---|
| 端到端视频延迟 | 与参考项目同级（百毫秒级，5G 公网场景可放宽到秒级可接受，需实测） |
| 断链恢复 | RTSP 采集线程自动重连；信令断线自动重连；WG 隧道保活（keepalive 25s） |
| 安全 | 数传/命令仅经可信链路（WG 隧道内）；公开端口尽量收敛 |
| 部署可维护 | 主摄固件自编（内置声卡驱动 + 5gipc 服务），刷机即用，不依赖 SD 卡 |
| 内存 | OpenIPC 91MB RAM 为硬约束，edge 进程需静态链接、精简缓冲 |

---

## 6. 关键约束（OpenIPC 主摄）

| 约束 | 说明 |
|---|---|
| RAM 91MB | edge 移植需评估峰值占用（参考项目 edge 在 2GB+ 板子上跑，缓冲可调） |
| NOR 16MB | kernel 2MB / rootfs 上限 8MB（当前 7.67MB），新增内容需谨慎 |
| 无 opkg / 无包管理器 | 一切依赖须编进固件或 SD 卡静态分发 |
| 无现成 ALSA | 内核需内置 USB Audio 驱动（自编固件，.ko 编入 rootfs） |
| 无现成 ffmpeg/gst/WebRTC 库 | 需交叉编译静态链接，或裁剪 edge 依赖 |
| busybox shell | 守护脚本按 ash 兼容编写 |

---

## 7. 方案定稿：edge 由 VPS 承担（方案 B，2026-09-11 确认）

### 7.1 决策结论

放弃"edge 全量上摄像头"（方案 A），改为 **VPS 跑参考项目 edge 的改造版**。原因：
- 摄像头 91MB 内存临界（majestic 已占 30-45MB，edge 全家再占 30MB+ 会爆）
- 对讲是 USB 声卡，majestic 帮不上，摄像头侧无论如何都要跑轻量音频进程——B 方案把它压到 ~8MB
- VPS edge 用 x86 编译，免去交叉编译 libdatachannel 的苦活
- 地面端零改动（VPS 跑同一套 edge 代码/协议/信令）

### 7.2 目标拓扑与端口分配

```
摄像头主摄(ssc338q, 10.55.0.2)                    VPS(182.92.77.180)               地面站
majestic H265 ──RTSP经WG──→  RtspCapture(url/url2) ──WebRTC──→ PC桌面站(零改动)
USB声卡 ┐                  ┌─ UdpAudioBridge :7213(上行Opus帧)   信令 wss://…:8443
音频桥 ┘─opus/UDP──→7213    ├─ talkSink → UDP→10.55.0.2:7214(下行)  TURN :3478
ttyS2→mavfwd ──→10.55.0.1:7212 ─→ MavlinkBridge(UDP模式:7212)
副摄(ssc338q, 10.55.0.3) ──RTSP经WG──→ RtspCapture url2
```

| 端口 | 用途 | 绑定 |
|---|---|---|
| 8443/tcp | WSS 信令（signaling） | VPS |
| 8091/tcp | HTTP 信令（备用） | VPS |
| 3478/udp+tcp + 49152-65535/udp | TURN 中继 | VPS |
| 7212/udp | MavlinkBridge 监听（mavfwd 推流目标 `10.55.0.1:7212`，**替代 mav_relay**） | VPS |
| 7213/udp | VPS edge 音频上行（摄像头音频桥发 Opus 帧 + 命令回执） | VPS |
| 7214/udp | 摄像头音频桥下行监听（VPS edge 发音频帧 + 命令） | 主摄 |

### 7.3 各端改造点

**VPS edge（参考项目 edge 改造，x86 编译）**：
- `main.cpp`：去掉 AudioCapture/AudioPlayer（ALSA）、RtspServer、GcsVideoForwarder；新增 `UdpAudioBridge`
- 音频上行：UDP 收摄像头 Opus 帧 → `streamer->onAudioFrame()`；下行：`talkSink` → UDP 转发摄像头
- `MavlinkBridge`：`connection: udp` + `fc_listen_port: 7212`（原生支持，零改）
- `RtspCapture`：url/url2 = `rtsp://admin:密码@10.55.0.2(或.3):554/stream=0`（FFmpeg，VPS apt 装）
- 音频命令（SET_MIC_GAIN/SET_MIC_HW_VOLUME/SET_VOLUME/GET_*）：转 UDP 文本命令转发摄像头音频桥，回执转下行 JSON
- 依赖：libdatachannel(静态) + FFmpeg + nlohmann；**不需要 Opus/ALSA**

**摄像头主摄（OpenIPC ssc338q）**：
- majestic：1080p30 H265（已验证）
- mavfwd：`-o 10.55.0.1:7212`（改 /etc/5gipc.conf）
- **音频桥（`audio/audio_bridge.c`，交叉编译）**，按 **2026-09-11 实机声卡拓扑** 设计：

  | 声卡 | 设备 | 角色 | 实测能力 |
  |---|---|---|---|
  | card0 Generalplus(1b3f:2008) | `hw:0,0` | 麦克风 capture only | mono, 44.1k/48k, S16_LE |
  | card1 USB2.0 Device | `hw:1,0` | 扬声器 playback only | stereo, **仅 48k**, S16_LE |

  链路：card0 采集 48k/mono → Opus 编码 → UDP 发 VPS:7213；UDP 收 VPS → 剥 RTP 头 → Opus 解码(mono) → **转 stereo** → card1 播放；增益/音量命令处理。**plughw 不可用**（plug 插件未打包），必须 `hw:0,0`/`hw:1,0`。
- 自编固件：内核 USB Audio 驱动（.ko 已编译验证）+ 内置音频桥/mavfwd/5gipc 服务

**摄像头副摄（OpenIPC ssc338q）**：
- 配 WG（VPS 加 peer 10.55.0.3/32）+ majestic 720p30，纯视频源

**地面端**：零改动，仅信令地址 `wss://182.92.77.180:8443/signaling` + 独立 room_id（如 `rcfpv5-rc`）

### 7.4 遗留风险

- 视频多一跳（摄像头→VPS→地面站），5G 公网下可接受，实测为准
- VPS 带宽：每路视频 ~2Mbps 上行（拉流）+ WebRTC 下行 ~2Mbps
- 音频编解码两段（摄像头↔VPS opus/UDP + VPS↔地面站 WebRTC Opus），延迟略增，对讲可接受

---

## 8. 范围外（本期不做）

- 手机 Flutter 地面端（用户明确仅桌面端）
- 副摄承担飞控/声卡/edge（只做纯视频源）
- 参考项目 desktop 之外的任何地面端新功能
- **RTSP 输出服务器 :8554（QGC/VLC 拉流）**——用户明确取消
- **GCS 视频转发 :5600（RTP/UDP + H.264 转码）**——用户明确取消
- SRT 传输升级（待交叉编译 armv7 ffmpeg+libsrt，另行立项）

---

## 9. 实施进度（2026-09-11，方案 B 执行中）

### 9.1 已完成

| 项 | 状态 | 说明 |
|---|---|---|
| 机位 USB 声卡可行性 | ✅ 实机验证 | 录音(card0)/播放(card1)全通，见 [README](README.md) |
| USB 音频内核模块 | ✅ 编译通过 | GH Actions `snd-modules` artifact（8 个 .ko + arecord + libasound.so + alsa.conf） |
| 需求/方案定稿 | ✅ | 本文件第 7 章（方案 B） |
| **VPS edge 代码** | ✅ 已交付 | `vps-edge/`：UdpAudioBridge + main.cpp 改造 + CMakeLists + config.json + deploy_vps.sh |
| **摄像头音频桥** | ✅ 已交付 | `audio/audio_bridge.c`（双卡适配）+ `audio/build_audio_bridge.sh` |
| workflow | ✅ 已入库 | `build-vps-edge.yml`、`build-audio-bridge.yml`（+ 既有 `build-audio.yml`） |

### 9.2 编译验证状态（GitHub Actions）

| workflow | 状态 | 备注 |
|---|---|---|
| build-audio-modules | ✅ 通过 | 已在跑（USB 驱动） |
| build-audio-bridge | 🔄 修复重跑 | 首跑失败：工具链残缺 autoreconf 抢在系统前 → 已修（PATH 系统优先 + ALSA/opus 官方包），已重新触发 |
| build-vps-edge | ❌ 待查 | 首次运行失败（~2min），libdatachannel v0.24.5 tag 已确认存在；失败点待看日志 |

### 9.3 待办（按序）

1. 定位 build-vps-edge 失败（日志 → 修复 → 重跑）
2. build-audio-bridge 通过 → 下载 `audio_bridge` 静态二进制
3. **VPS 部署**（182.92.77.180）：signaling(coturn+8443) → VPS edge(x86 二进制+7212/7213) → 副摄 WG peer(10.55.0.3/32)
4. **设备装机**：主摄装 audio_bridge + mavfwd 改 7212 + 开机自启；副摄刷固件+配 WG
5. **端到端联调**：双摄视频 + 数传 + 对讲 + 看门狗

### 9.4 关键实测结论（固化）

- 声卡拓扑：card0=mic(mono) / card1=speaker(**stereo 仅48k**) / **plughw 不可用** → 音频桥用 `hw:0,0`+`hw:1,0`，播放 mono→stereo
- 设备 glibc 为 buildroot-gcc-13.3.0(armhf)，与 openipc 工具链匹配 → 动态链接可用（arecord 已用）；audio_bridge 保持静态（只用 hw 设备，无插件依赖）
- 工具链自带 autoreconf 残缺（缺 Autom4te Perl 模块）→ 构建脚本必须 `PATH="/usr/bin:/bin:$TC_DIR/bin:$PATH"` 且尽量用官方 release 包（自带 configure）
